server:
  port: 8080

spring:
  application:
    name: mortgage
  datasource:
    url: jdbc:h2:mem:mortgage-db
    driverClassName: org.h2.Driver
    username: test
    password: test
  h2:
    console:
      enabled: true
      path: /h2-console
management:
  endpoints:
    web:
      exposure:
        include: health, info
